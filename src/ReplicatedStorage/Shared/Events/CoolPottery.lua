local ReplicatedStorage = game:GetService("ReplicatedStorage")

local Red = require(ReplicatedStorage.Shared.Red)

return Red.Function("CoolPottery", function(Player, stationId: string)
    -- Server validates and handles placing pottery on cooling table
end)
