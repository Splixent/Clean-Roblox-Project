local ReplicatedStorage = game:GetService("ReplicatedStorage")

local Red = require(ReplicatedStorage.Shared.Red)

return Red.Function("FirePottery", function(Player, stationId: string)
    -- Server validates and handles placing pottery in kiln for firing
end)
