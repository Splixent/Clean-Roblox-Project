local ReplicatedStorage = game:GetService("ReplicatedStorage")

local Red = require(ReplicatedStorage.Shared.Red)

return Red.Function("CollectKilnPottery", function(Player, stationId: string, slotIndex: number)
    -- Server validates and handles collecting fired pottery from kiln
end)
