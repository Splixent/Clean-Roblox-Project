local ReplicatedStorage = game:GetService("ReplicatedStorage")

local Red = require(ReplicatedStorage.Shared.Red)

return Red.Function("CollectPottery", function(Player, stationId: string, slotIndex: number)
    -- Server validates and handles collecting cooled pottery from cooling table
end)
